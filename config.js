// Конфигурация фронтенда. Меняй только здесь.
window.APP_CONFIG = {
	// Адрес бэкенда с оплатой (server/). Пример: "https://api.example.ru"
	// Пустая строка = тот же домен.
	apiBase: "",

	// Цена Premium (без рекламы), в рублях. Должна совпадать с сервером.
	premiumPriceRub: 149,

	// Реклама. Yandex РСЯ: вставь свои ID блоков после одобрения сайта.
	// Пока ID пустые — вместо рекламы показывается серая заглушка.
	ads: {
		provider: "yandex", // "yandex" | "none"
		blockIdTop: "", // например "R-A-1234567-1"
		blockIdBottom: "", // например "R-A-1234567-2"
	},
}
